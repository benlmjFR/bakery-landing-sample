export type LocationData = {
  name: string;
  image?: string;
  address: string;
  zip: string;
  city: string;
  hours: string;
  closedDay: string;
  email: string;
  phone: string;
  mapEmbedUrl: string;
};